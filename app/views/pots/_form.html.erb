<%= form_with(model: pot, local: true) do |form| %>
  <% if pot.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pot.errors.count, "error") %> prohibited this pot from being saved:</h2>

      <ul>
      <% pot.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :pot_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
